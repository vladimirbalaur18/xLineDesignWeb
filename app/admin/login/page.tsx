import AdminLoginWrapper from "./AdminLoginWrapper";

/**
 * Admin login page component that renders the admin login UI.
 *
 * Returns the `AdminLoginWrapper` element which contains the full admin login interface.
 *
 * @returns The JSX element for the admin login page.
 */
export default function AdminLoginPage() {
  return <AdminLoginWrapper />;
}
